# Main Class Demo

TeamCode stores the demo java project a user might upload, which has been converted into a jar stored in the TeamCode folder by the Android Studio plugin
vrs_library stores the "forked" FTC-SDK with some initial classes for testing
org.json stores the json reading package that is used to convert user-uploaded robot CAD files into the simulator
